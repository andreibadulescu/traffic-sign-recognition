<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="TrafficSignRecognitionProject.Views.TrainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TrafficSignRecognitionProject.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <StackPanel Spacing="30" HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock x:Uid="Train_InitialMessage" Style="{ThemeResource BodyTextStyle}" HorizontalTextAlignment="Center" />
            <CheckBox x:Uid="Train_ContentCheck" IsChecked="{x:Bind ViewModel.ContentIsYOLOAnnotated, Mode=TwoWay}" HorizontalAlignment="Center" />
            <Button x:Uid="Train_SelectButton" Name="TrainSelectButton" Style="{StaticResource AccentButtonStyle}" Command="{x:Bind ViewModel.PickFolderCommand}" CommandParameter="{x:Bind TrainSelectButton}" HorizontalAlignment="Center" />
            <TextBlock x:Name="StatusWindow" Text="{x:Bind ViewModel.ExitMessage, Mode=OneWay}" Visibility="{x:Bind ViewModel.ShowExitMessage, Mode=OneWay}" HorizontalTextAlignment="Center" Style="{ThemeResource BodyStrongTextBlockStyle}" />
        </StackPanel>
    </Grid>
</Page>
